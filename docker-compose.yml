services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: promptlib
      POSTGRES_PASSWORD: promptlib
      POSTGRES_DB: promptlib
    ports:
      - "5432:5432"
    volumes:
      - promptlib_pgdata:/var/lib/postgresql/data

volumes:
  promptlib_pgdata:
